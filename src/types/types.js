import { imageProps } from './image';
import { linkProps } from './link';
import { seoProps } from './seo';

export const types = {
    imageProps: imageProps,
    linkProps: linkProps,
    seoProps: seoProps,
};